In *code* folder the file containing the project is ***Tasks.ipynb***, while inside *notebooks* folder there are some model implementations that we've decided to separate from the main one to keep the file cleaner.

In *notebooks_html* folder there are the same files in .html format.